__pycache__/
*.py[cod]

# Nuitka / pyside6-deploy build artefacts
dist/
build/
main.build/
main.dist/
main.onefile-build/
deployment/
build.log
nuitka-crash-report.xml

# User-specific rule configs + captured template images. The folder
# itself is preserved via templates/.gitkeep so the app can write into it.
templates/*
!templates/.gitkeep

# Claude Code per-repo settings
.claude/

# Runtime logs from restart.ps1 (stdout/stderr capture)
logs/

# Local browser smoke-test artifacts
.playwright-cli/
